#include <stdio.h>
int main() {
    int price[5], i, key, found = 0;
    for (i = 0; i < 5; i++)
        scanf("%d", &price[i]);
    scanf("%d", &key);
    for (i = 0; i < 5; i++)
        if (price[i] == key) found = 1;
    if (found) printf("Found");
    else printf("Not Found");
    return 0;
}
