#include <stdio.h>
int main() {
    char s[50];
    int i, count = 0;
    scanf("%s", s);
    for (i = 0; s[i]; i++)
        if (s[i]=='a'||s[i]=='e'||s[i]=='i'||s[i]=='o'||s[i]=='u')
            count++;
    printf("Vowels = %d", count);
    return 0;
}
