#include <stdio.h>
struct mobile {
    char model[20];
    int price;
};
int main() {
    struct mobile m;
    scanf("%s %d", m.model, &m.price);
    printf("%s %d", m.model, m.price);
    return 0;
}
