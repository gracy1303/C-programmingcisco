#include <stdio.h>
#include <string.h>
int main() {
    char pass[20];
    scanf("%s", pass);
    if (strlen(pass) >= 8)
        printf("Valid Password");
    else
        printf("Invalid Password");
    return 0;
}
