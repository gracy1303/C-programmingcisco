#include <stdio.h>
struct emp {
    int id;
    char name[20];
};
int main() {
    struct emp e[3];
    int i, key;
    for (i = 0; i < 3; i++)
        scanf("%d %s", &e[i].id, e[i].name);
    scanf("%d", &key);
    for (i = 0; i < 3; i++)
        if (e[i].id == key)
            printf("%d %s", e[i].id, e[i].name);
    return 0;
}
