#include <stdio.h>
int main() {
    int sale[7], i, total = 0;
    for (i = 0; i < 7; i++) {
        scanf("%d", &sale[i]);
        total += sale[i];
    }
    printf("Total Sales = %d", total);
    return 0;
}
